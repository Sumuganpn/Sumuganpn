#include<stdio.h>
#include<conio.h>

void main()
  {
     clrscr();
     int a,b;

     printf("ENTER MARK 1: ");
     scanf("%d",&a);
     printf("ENTER MARK 2: ");
     scanf("%d",&b);

     printf("\na>100 && b<50 is %d\n", (a<100) && (b<50) );
     printf("a==100 || b>100 is %d\n", (a==100) || (b>100) );
     printf("!(a==100) && b!=10 is %d\n", !(a==100) && (b!=10) );
     printf("!(a!=50) || b!=400 id %d\n", !(a!=50) && (b!=400) );
     printf("a>=100 && b<50 && a!=50\n", (a<=100) && b<50 && a!=50);

     getch();

  }
